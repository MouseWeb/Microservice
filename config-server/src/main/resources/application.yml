
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/MouseWeb/Microservice/tree/master/microservice-repo/
          search-paths: fornecedor